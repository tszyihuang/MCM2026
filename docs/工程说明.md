# 工程说明（README 详细版）

> 本文是仓库首页 [`../README.md`](../README.md) 的**详细工程版**，保留参数标定、留档对照、
> 口径提醒等逐条记录。**文中出现的路径均相对仓库根目录。**
> 想先了解项目概况与快速上手，请先读 [`../README.md`](../README.md)。

---

# 2026 高教社杯 B 题 · 无线电干扰源环境模拟器与机器狗程序

本仓库是 **B 题「无线电干扰源的快速自动定位与清除」** 的完整自研工具链，分四层：

| 层 | 目录 | 说明 |
| --- | --- | --- |
| **环境** | `simulator/` | 严格按题目正文、附件1《模拟器使用说明》、附件2《模拟器通信接口说明及编程指南》实现的自建模拟器，可本地复现四个测试模块与全部协议细节 |
| **策略** | `robotdog/solver/` | 问题3 求解器 **`sweeper`**、问题4 求解器 **`sweeper4`** |
| **通信** | `robotdog/` | `RobotClient`：HTTP+JSON 客户端（重试 / 幂等 / 现实预算 / 行为日志） |
| **验证** | `tools/`（含 `tests/` 子包）、`docs/` | 自动化用例、冒烟测试、独立赛题符合性审计、验收与实测报告 |

> **本工程提供两条求解器路线**：问题3 的 `sweeper`（全向源）与问题4 的 `sweeper4`（含定向源）。
> 两者互不依赖，各自有独立的建模文档、评测入口与回归测试。

---

> [!WARNING]
> ### 计时口径：虚拟 100 小时，现实至多 20 分钟（最容易搞错的一点，先读这里）
>
> **虚拟环境里有 100 小时的虚拟时间，而策略程序自己的运行时间至多 20 分钟。**
> 这是两个**相互独立**的上限，不是同一个数字的两种说法：
>
> | | 上限 | `/enter` 返回的字段 | 由什么决定 |
> | --- | --- | --- | --- |
> | **虚拟世界活动时长** | **100 小时 = 360000 s** | `max_virtual_duration_s` | 机器狗**移动 / 检测 / 清除**累计消耗的虚拟秒数 |
> | **策略程序运行时间** | **20 分钟 = 1200 s** | `remaining_real_duration_s` | **现实墙钟**，从 `/enter` 成功那一刻开始计 |
>
> 关键含义：**现实 1 秒可以推进任意多虚拟秒**。附件2 §1.4 明确「每次检测的 5 秒只
> 增加虚拟时间，**不要求在现实中等待 5 秒**」，所以 20 分钟现实预算内可以累计出
> 数万虚拟秒。
>
> 三条推论，写程序前务必确认：
>
> 1. **先触发的几乎总是现实的 20 分钟**。100 小时虚拟上限只是防止程序死循环的兜底，
>    正常运行撞不到 —— 本项目每局只用掉 360000 s 的约 1%。
> 2. **不要写 `if 已用虚拟时间 > 1200: 退出`**。那是把虚拟时钟当成现实预算，
>    历史上正因此让程序只跑了 0.4 s 现实时间就自行收尾（见 `docs/验收报告.md` §6.1）。
> 3. 还存在第三个上限：**25 分钟测试窗口**（`window_left_s`，从倒计时结束算起）。
>    它与上述两者相互独立，三者取**最早到达者**结束测试。
>
> 预算判断一律用 `/enter` 返回的 `remaining_real_duration_s`（**现实**口径）。

## 目录结构

```
simulator/                模拟器本体 (纯标准库)
  core.py                 物理规则、虚拟计时、案例生成、报告与加密行为日志
  server.py               HTTP+JSON 通信层（状态码、幂等、并发串行闸门、严格校验）
  session.py              登录/时间校验、四个测试模块、25 分钟窗口、日志队列与导出
  gui.py                  模拟器界面（测试控制台 + 指令与反馈 + 日志列表）
  __main__.py             命令行入口: python -m simulator

robotdog/                 机器狗程序
  consts.py               题目物理常量（唯一来源）
  geometry.py             方位角 / 交会 / 加权残差拟合
  client.py               RobotClient: HTTP 客户端（重试 / 幂等 / 现实预算 / 行为日志）
  solver/                 问题3 求解器 (见 solver/README.md)  [需 numpy]
    world.py              进程内精确复刻环境 (真值与可观测信息严格分离)
    belief.py             信念（网格 / 射线样本 / Gauss-Newton 三表示）
    belief4.py            问题4 信念 (位置三表示 + 源型 λ + 定向方向 ψ 的 36 分箱后验)
    ops.py                动作原语: pursue / probe / 候选点 / 收益评估
    sweeper.py            "清除即探测"规划器
    scan4.py               问题4 补扫点集几何 (六边形网格 / 环 / Held-Karp 巡游 / 覆盖审计)
    sweeper4.py            问题4 规划器: 16 站骨架 + 中继测量 + 交会定位 + 在线路线重优化 (当前发布)
                           22 站零漏检档只差 PARAMS['ring_def'] 一行; 上一版 18 站档与初版规划器已移出
                           (见 robotdog/README.md「整理记录」)
    deploy.py             机器狗程序部署入口: python -m robotdog.solver.deploy --problem 3|4
    eval.py               进程内评测入口: python -m robotdog.solver.eval
    e2e_test.py           真实进程 + HTTP 端到端验收
    oracle_route.py       已知路线下界探针（论文取数）
    opt1_port.py          与 Desktop/opt1 单文件沙盒对照的适配层 (实验用, 不在发布路径上)
    opt1_deploy.py        上者的部署入口 (tools/run_batch.py --robot-module robotdog.solver.opt1_deploy)

tools/                    工程与验收工具 + 测试套件（不被策略导入）
  smoke_test.py           一键冒烟测试（协议/计时/并发/保密/真实进程端到端）
  spec_audit.py           独立赛题符合性审计（不依赖 tests/ 子包的既有用例；失败退出码非 0）
  candidates.py           批量评测（进程内口径 knows_total=True）
  eval_deploy.py          批量评测（**部署口径** knows_total=False，正式测试口径）
  profile_run.py          阶段剖析：每个阶段的动作数/秒/移动（--deploy 切口径）
  move_bound.py           移动下界：真值精确 TSP vs 策略实际移动
  verify_candidate.py     交付验收：确定性 + 双口径成绩
  pareto_front.py         帕累托前沿：清除比例 vs 平均定位清除时间
  pareto_ci.py            前沿的配对置信区间：逐局配对 + 按案例 bootstrap（论文 §5.2）
  make_paper_figures.py   论文插图生成 (中文, 全部可复现)
  figstyle.py             论文插图统一配色 (蓝-粉 8 色方案, 三份出图脚本共用)
  run_batch.py            演练/正式测试批处理（导出日志与统计表，支持 --jobs 多核并行）
  bench_cases.py          小批量多核对局基准（--serve 常驻模式，反复回归用）
  perf_bench.py           模拟器性能基准（微基准 + 多核对局 + 行为指纹对比）
  decision_fingerprint.py 逐动作决策指纹（重构安全网）
  oracle.py               清除个数上界估计（虚拟时间口径，见 docs/验收报告.md §4.3）
  eval_q4.py              问题4 批量评测（进程内 / --deploy 部署口径）
  tune_q4.py              问题4 参数扫描（多旋钮网格）
  quick_check.py          手工冒烟: 对运行中的模拟器跑一遍附件示例指令序列
  run_sim.py              手工调试: 启动一个会话并保持运行
  probes/                 问题4 覆盖审计 / 失败归因 / 阶段剖析 / 论文配图探针
  tests/                  自动化测试套件（标准库 unittest）
    harness.py            测试夹具（可注入假时钟的模拟器实例 + HTTP 客户端）
    test_protocol.py      协议层：状态码/方法/请求体校验/幂等/并发 409/时间预算
    test_physics.py       物理规则与虚拟计时（附件1 表2、附件2 第 10 节的完整时序示例）
    test_scenario.py      四个测试模块、倒计时、日志导出与加密、正式测试真值屏蔽
    test_acceptance.py    真实进程端到端：问题3 演练 + 正式测试真值屏蔽与加密日志
    test_solver_equiv.py  求解器与 simulator.core 的逐动作物理等价性
    test_q4.py            问题4: 定向源规则 / 覆盖几何 / 端到端指标与确定性

docs/                     文档 (见 docs/README.md 的完整索引)
  README.md               docs 索引
  **问题3论文.md**         问题3 的建模与算法（论文写法）
  **问题4论文.md**         问题4（含定向源）的建模与算法（论文写法）
  **问题一与问题二论文.md** 问题一/二 的定稿解法（含精确直径优化, 数字由 tools/probes/q12_review.py 复现）
  模拟器设计说明.md        模拟器逐条对照题目与附件的实现口径
  模拟器使用说明.md        命令行参数、与附件2 的对应关系、测试与验收命令
  验收报告.md             自动化测试结果、计时口径勘误、已修复问题清单
  figures/                论文插图 (统一配色, 见 tools/figstyle.py): 问题1/2 4 张 + 问题3 4 张 (正文全部引用) + 问题4 5 张 (见快速开始)
  experiments/            实验存档索引（原始报告已丢失，见该目录 README.md）

data/                     运行时产物与对照证据（见 data/README.md）
  logs/                   行为日志留档：正式测试 8 组（问题3 3 局 + 问题4 5 局）+ 被论文引用的演练档
  robot_logs/             与正式测试同 ID 的机器狗侧行为日志（JSONL，16 个）
  reports/                评测结果 JSON（13 份，清单见 data/README.md）
附件/                     题目附件1、附件2 原文
```

## 快速开始

模拟器与部署程序只依赖 **Python 标准库（3.9+）**；
求解器的进程内评测需要 `numpy`（`pip install numpy`）。

```powershell
# 1) 启动模拟器（机器狗接口 :2026，操作界面 :2027）
python -m simulator --team-id <参赛队号>

# 2) 浏览器打开界面，选择测试模块并确认开始；倒计时结束后运行机器狗程序
python -m robotdog.solver.deploy --team-id <参赛队号> --problem 3   # 问题3
python -m robotdog.solver.deploy --team-id <参赛队号> --problem 4   # 问题4
```

一键自检：

```powershell
python tools\smoke_test.py --full      # 冒烟 + 完整测试套件
python tools\spec_audit.py             # 独立赛题符合性审计 (115 项断言, 失败时退出码非 0)

# 问题3: 两种口径 (不可混比)
python tools\candidates.py  --modules robotdog.solver.sweeper --seeds 9500-11499 --jobs 6
python tools\eval_deploy.py --module  robotdog.solver.sweeper --seeds 9500-11499 --jobs 6

# 问题4: 定向源
python tools\eval_q4.py --seeds 9500-9999   --jobs 16
python tools\eval_q4.py --seeds 20000-20999 --jobs 16

# 剖析与下界
python tools\profile_run.py --module robotdog.solver.sweeper --seeds 9500-9529 --deploy
python tools\move_bound.py --seeds 9500-9539 --jobs 3

# 端到端 (真实进程 + HTTP)
python -m robotdog.solver.e2e_test --runs 3
python -m robotdog.solver.e2e_test --runs 3 --problem 4

# 论文插图 -> docs/figures/
python tools\probes\q12_make_figures.py      # 问题1/2 插图 4 张
python tools\make_paper_figures.py          # 问题3 正文插图 4 张
python tools\probes\q4_make_figures.py      # 问题4 插图 5 张

# 正式测试 (真值屏蔽 + 加密日志)
python tools\run_batch.py --problem 3 --module formal --runs 3 --jobs 3 --countdown 0
python tools\run_batch.py --problem 4 --module formal --runs 5 --jobs 5 --countdown 0
```

## 策略成绩：问题4（含定向源）

当前问题4 策略 = **中继测量 + 在线路线重优化方案**（`robotdog/solver/sweeper4.py`）：
**16 站巡游骨架**（原点 + 内环 985 m×5 + 外环 1850 m×10，速度档）
+ 加权最小二乘交会定位 +
**中继测量**（把"第 2 条示向度"顺路插在腿内：`|A−M|+|M−B| = |A−B|`，零额外移动，
交会定位提前一整站）+ **每一步按"剩余站点 ∪ 已定位待清目标"重规划路线**
（计划稳定性 + cheapest insertion + 2-opt/Or-opt）。决策层与单文件版
`Desktop/solver_q4.py` **逐字一致**（`tools/probes/q4_opt1_parity.py` 在 seed
9500-9549 上逐位一致 50/50），文件下半部分只是主工程适配层。

标定集 **seed 9500-9999（500 局）**、独立测试集 **seed 20000-20999（1000 局，未参与标定）**、
另加全新集 **30000-30999 + 40000-40999（2000 局）**，策略确定性执行。口径为**部署**
（`knows_total=False`：正式测试看不到真值总数）。

| 数据集 | 清除比例 | 全清局 | 平均定位清除 | 每局移动 | 每局检测 | 定向源清除 |
| --- | --- | --- | --- | --- | --- | --- |
| 标定 9500-9999（500 局 / 6511 源） | **0.9992** | 495/500 | **424.9 s/源** | 20079 m | 215.3 | 3219/3224 |
| 独立 20000-20999（1000 局 / 13042 源） | **0.9992** | 990/1000 | **422.3 s/源** | 19971 m | 213.7 | 6470/6480 |
| 全新 30000-30999 + 40000-40999（2000 局 / 26005 源） | **0.9992** | 1978/2000 | **421.6 s/源** | 19885 m | 213.6 | 12982/13004 |
| 真实进程 + HTTP（正式模块，seed 20000-20039，40 局 / 526 源） | **1.0000** | 40/40 | 413.1 s/源 | 19800 m | 210.3 | — |

指标核对：**清除比例 99.92% ≥ 98% ✅**（余量 1.92 pp；3500 局 45558 个源漏 37 个，
37 局各漏 1 个）；**平均定位清除 421.6 s/源 ≤ 600 s/源 ✅**（余量 30%）。
程序现实运行 **0.1~0.3 s**（上限 1200 s），每局虚拟时间约 5350 s（上限 360000 s）。

**与留档版本对照**（同批种子）：

| 数据集 | `sweeper4_legacy`（覆盖巡游） | `sweeper4_ring18`（18 站） | 22 站中继（零漏检档） | 本文 `sweeper4`（16 站中继，当前发布） |
| --- | --- | --- | --- | --- |
| 标定 500 局 | 0.9960 / 474 全清 / 545.3 s/源 | 0.9995 / 497 全清 / 463.7 s/源 | 1.0000 / 500 全清 / 451.2 s/源 | **0.9992 / 495 全清 / 424.9 s/源** |
| 独立 1000 局 | 0.9929 / 910 全清 / 549.8 s/源 | 0.9994 / 992 全清 / 461.9 s/源 | 1.0000 / 1000 全清 / 450.6 s/源 | **0.9992 / 990 全清 / 422.3 s/源** |

### 精度 ⇄ 速度：16 站 vs 22 站（2026-09-13 实测）

题目只要求 **清除比例 ≥ 98%**，所以"少布点、跑快点"是可选项。在标定 500 + 独立 1000 +
全新 2000 = **3500 局**上扫站数这条轴，唯一有效的杠杆就是**站点集几何**：

| 配置（`ring_def`） | 站数 | 清除比例 | s/源 | 相对 22 站 | 3 局均值 < 98% 风险 |
| --- | --- | --- | --- | --- | --- |
| `((0,1),(985,7),(1850,14))`（零漏检档，可切） | 22 | **1.0000** | 450.6 | — | **0%** |
| `((0,1),(985,6),(1850,12))` | 19 | 1.0000 | 443.2 | −1.6% | 0% |
| `((0,1),(985,5),(1850,10))` **← 当前发布** | 16 | 0.9992 | **422.3** | **−6.3%** | 3.1% |
| `((0,1),(985,4),(1850,8))` | 13 | 0.9903 | 405.0 | −10.1% | ≈26% |
| `((0,1),(985,3),(1850,6))` | 10 | 0.9581 | 396.1 | −12.1% | **不可用**（< 98%） |

（清除比例与 s/源取独立集 seed 20000-20999 共 1000 局；风险按 3500 局逐局重抽样 200000 次。）

减少**观测**的旋钮（`zero_inner` / `zero_ring_stride` / `max_chan_station` / `early_k` /
`skip_far`）以及 `relay`、`fix_sigma_ok` 一律**无效或反向**：砍掉"空频道补测"会漏源，
漏掉的源要在收尾阶段重新搜，时间反而更长（`zero_inner=1` 时 0.8148 / 559.8 s/源）。
时间构成上 **98.6% 的移动发生在 survey 阶段**（在线 TSP 同时排"站点"与"已发现待清源"），
所以决定时间的不是检测预算，而是"必须走到每个源 20 m 内"的物理里程
（全知下界：1 测 1 清 = 151 s/源、每源 2 测 = 286 s/源）。**再往下压清除率收益极小**：
从 100% 一路放宽到 91%，总共只快 13%。

> ### ⚠ 当前用 16 站：放弃了"零漏检"的几何保证（22 站是零漏检档）
>
> 定向源只朝半个平面辐射，"无论它朝哪边都至少有一站落在 ψ±90° 内"等价于
> **从源看过去的站点最大角隙 ≤ 180°**。几何审计（`tools/probes/q4_opt1_layout.py`）：
>
> | 布局 | 骨架 | 最坏角隙 | 判据 | 抽样 26 万个源中"任何站都看不见" |
> | --- | --- | --- | --- | --- |
> | 22 站（985@7 + 1850@14）（零漏检档，可切） | 17687.6 m | **178.99°**（该点可见 3 站） | ✅ 零漏检 | **0 个（0.000%）** |
> | 16 站（985@5 + 1850@10）**← 当前发布** | 15603.5 m | **360.00°**（该点只剩 1 站） | ❌ 存在漏检方向 | 236 个（**0.091%**） |
>
> 外环减到 13 点即退化为 180.97°，且**≤19 站没有任何布局能跨过 180°**（随机搜索 2500 组
> 候选全部落空）—— 22 站是"可证零漏检"的临界布局（实测 3500 局零漏源）。
> 16 站的 0.08% 漏源率**正是这个几何缺口的直接后果**，不是执行失误：37 个漏源全是
> "任何站点都看不见"的贴边外向定向源。换成现场口径就是 **3 局平均清除比例 < 98% 的
> 概率 3.1%**（22 站档 0%，5 局 0.11%）。时间侧 16 站略稳一点（逐局 s/源 标准差 73.7、
> p99 593.9；22 站 87.6 / 625.3）。要拿回 0% 漏源风险就切回 22 站，代价是 +6.3% 时间。

端到端（真实模拟器进程 + HTTP + 真值屏蔽，`tools/run_batch.py --problem 4 --module formal`）
按当前 16 站档实测 40 局（seed 20000-20039）：**100% 清除、413.1 s/源、现实运行 0.13 s**
（上限 1200 s，留档 `data/reports/q4_ship16_http40.jsonl`）；22 站档的 20 局记录为
100% 清除 / 现实运行 0.2 s（`data/reports/q4_ship22_http20.jsonl`）。
走线审计（`q4_opt1_route.py --oracle`，seed 9500-9504）显示：16 站骨架 15 603.5 m，
总冗余 = 全知下界的 **+11.6%**（22 站档是 +7.7%）。

> ### ℹ 文档口径提醒
> `docs/问题4论文.md` 与 `docs/figures/q4_*.png` 仍按**上一版 18 站 opt1 方案**撰写
> （正文的 463.7 s/源、18 站布局、漏源讨论都是那一档的数字）。本次替换只动了策略与
> 评测口径；要让论文/插图与发布策略一致，需重跑 `tools/probes/q4_make_figures.py`
> 与第 4 章数字（探针与评测入口已全部指向新策略，可直接复现）。

明细：`data/reports/q4_ship16_calib500.json` / `q4_ship16_holdout1000.json` /
`q4_ship16_fresh2000.json` / `q4_ship16_http40.jsonl`（16 站发布档）与
`q4_ship_relay_*.json` / `q4_ship22_http20.jsonl`（22 站零漏检档对照）；
阶梯与消融 `q4_opt1_ladder500.json`，布局审计 `tools/probes/q4_opt1_layout.py`，
走线审计 `q4_opt1_route.py --oracle`，放宽—提速前沿 `q4_relax_frontier.md`。

## 策略成绩：问题3

标定集 **seed 9500-11499（2000 局，约 2.6 万个源）**，策略确定性执行。

> ### ✅ 当前发布参数 = **保障模式 λ=0.00**（≈378 s/源）：停机即不漏源
>
> `build_cfg()` 现为 `coverage_finish=True, hunt_mass_min=0.00`：信念质量扫完之后，
> 仍按 **1000 m 覆盖判据**把每个未清除频道补齐 —— 题目给定 R ≥ 1000 m，所以"该频道
> 在圆域内处处被测过它的测点以 1000 m 覆盖"⇒"源必被发现"，**停机即不漏源**。
> 2000 局标定集实测清除比例 **1.0000**（2000/2000 全清），3 局正式测试失败概率 **0%**。
> 代价是虚拟时间：速度优先档（`coverage_finish=False, hunt_mass_min=0.05`）只要
> 281.6 s/源，但清除比例 0.9988、**3 局跌破 98% 这条线的概率约 4.6%**（3 局只有约 39 个源，
> 漏 1 个即 38/39 = 0.974）。本项目取保障模式：多花约 34% 的虚拟时间换 0% 漏源风险。
>
> ### ⚠ 先看口径，再看数字：进程内与部署**不可直接比较**
>
> `robotdog/solver/eval.py` 走**进程内**（`knows_total=True`：能看到真值总数，清完最后一个源
> 立刻停）；`robotdog/solver/deploy.py` 走**部署**（`knows_total=False`：**正式测试隐藏真值总数**，
> 只能靠信念判据收手，因此还要多花时间确认"真的清完了"）。
> **被打分的是部署口径**，它天生比进程内慢。

| 口径 | 清除比例 | 全清率 | 平均定位清除 | 移动 |
| --- | --- | --- | --- | --- |
| **部署（正式测试口径，被打分）** | **1.0000** | 2000/2000 | **377.5 s/源** | 18296 m |
| 进程内（知道总数，清完即停） | 1.0000 | 2000/2000 | 242.9 s/源 | 11238 m |

独立 / 全新种子集（从未参与标定）部署口径同样是 **1.0000**：seed 8000-8999 + 20000-21999
（3000 局）**1.0000 / 378.9 s/源**，seed 30000-31999（2000 局）**1.0000 / 377.1 s/源**，
合计 7000 局全清。端到端（真实模拟器进程 + HTTP + 真值屏蔽）20 局 **100% 清除**，
现实程序运行时间 **0.1~0.3 s**（上限 1200 s，留档 `data/reports/q3_cover_http20.jsonl`）。

> 对照（同一批 2000 局）：**速度优先 λ=0.05** 为 281.6 s/源 / 0.9988 / 失败率 4.6%，
> **λ=0.30** 为 250.4 s/源 / 0.9907 / 29.2%（留档 `q3_deploy_speed250.json`）。
> 两种口径的分析见 `docs/问题3论文.md` §5.2（表 5/表 6 与图 3/图 4）；论文正文的
> "运行口径说明"与结论已同步为保障模式，但表 5/表 6 的绝对数字取自更早一批参数
> （未含 2026-09-13 的 `sweep_pi_min` / `leg_stop` 收紧），比值与结论不受影响。

## 关键约定

* **计时口径**：见文首标注。虚拟上限 360000 s 仅防死循环，现实 1200 s 才是约束；
  预算判断一律用 `/enter` 返回的 `remaining_real_duration_s`。
* **并发**：附件2 表2 规定「并发发送了不同动作 → 409」。模拟器内置串行闸门，
  引擎内同时只执行一个动作；网络重试须复用原 `request_id` 与请求内容。
* **保密**：正式测试的案例真值在任何界面、完成提示与控制接口（`/api/state`）中都不显示，
  只写入加密行为日志（< 2 MB）。
* **依赖边界**：`simulator/`、`robotdog/`（通信层）、`tools/`（含 `tests/` 子包）只依赖标准库；
  只有 `robotdog/solver/` 的进程内评测需要 numpy，**部署程序只用标准库**。
* **两条策略互不依赖**：`sweeper`（问题3）与 `sweeper4`（问题4）之间没有共享的策略状态；
  共用的是通信层、常量与进程内环境。`sweeper4` 甚至不建信念（`_NullBelief`）——
  它只用几何与示向度。
* **常量单一来源**：题目物理常量只在 `robotdog/consts.py` 定义一份。
  `simulator/core.py` 刻意**不**共享它——模拟器是对题目的独立复现，
  两份常量各自独立，逐位等价性测试（`tools/tests/test_solver_equiv.py`）才有意义。
* **可复现**：策略与评测全部确定性（射线重采样用固定相位而非无播种随机数），
  同一命令跑两次逐位一致。

## 文档

| 文档 | 内容 |
| --- | --- |
| `docs/README.md` | **docs 索引**：目录里每份文档的现状与用途 |
| **`docs/问题3论文.md`** | **问题3 的建模与算法**（论文写法）：问题分析、模型建立、算法设计、结果与分析 |
| **`docs/问题4论文.md`** | **问题4 的建模与算法**（论文写法）：角隙覆盖判据与"速度优先"布局放宽、加权交会定位、在线路线重优化、走线冗余归因、消融与指标达成分析 |
| **`docs/问题一与问题二论文.md`** | **问题一、问题二 的定稿解法**：直径圆覆盖与精确直径优化 |
| `robotdog/solver/README.md` | 求解器总说明：参数标定、阶段剖析与工程要点 |
| `docs/模拟器设计说明.md` | 逐条对照题目正文与附件的实现口径、需自行假设的部分 |
| `docs/模拟器使用说明.md` | 命令行参数、与附件2 的对应关系、测试与验收命令 |
| `docs/验收报告.md` | 自动化测试结果、计时口径勘误、已修复问题清单 |
| `docs/experiments/README.md` | 实验存档索引：原始报告已丢失，这里列出每条结论的代码/数字还活在哪里 |
| `data/README.md` | 运行时产物与证据文件清单 |
